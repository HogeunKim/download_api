- Go 1.22 버전 이상의 표준 라이브러리(net/http) 패턴을 준수한다.
- OS 종속적인 기능은 `_windows.go`와 `_linux.go`로 분리하여 빌드 태그를 사용한다.
- 모든 파일 경로는 `path/filepath` 패키지를 사용하여 결합한다.
- RESTful 원칙에 따라 HTTP Method(GET, POST, PUT, DELETE)를 명확히 구분한다.
